1. var let const difference

var is function scoped. It can reassign and redeclare. it is old way.
let is block scoped. It can reassign but it cannot redeclare. It value can change
const is block scoped. It cannot reassign and redechange It value cannot change.

2. Spread operator
The spread operator is used to expand or copy elements of an array or object.

3. Difference between map, filter and forEach
map use transform each element
filter use select elements based on condition
forEach use Loop through elements

4.arrow function
An arrow function is a shorter way to write function in javascript (ES6) 

5. template literals
Template literals allow to write atrings with variables easily using backticks(``)